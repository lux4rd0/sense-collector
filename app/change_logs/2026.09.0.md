# 2026.09.0 — 2026-09-06

Maintenance and hardening. No config, API or data-shape changes; no migrations.

## Issues Closed

- SENSECOLLE-38 — swallowed-exceptions burn-down — narrowed 26 broad `except Exception` sites to the errors that can actually occur there; 6 last-resort nets keep a broad catch with an inline `# swallowed-exceptions:` reason. Sweep now reports 0 active / 6 waived. (02adaf4)
- SENSECOLLE-39 — Wire the coverage ratchet and close the client.py gap — added `pytest-cov`, piped `make test` through `luxlint --coverage-ratchet`, floor set at the honest measured 70. Coverage 41% → 70%, tests 52 → 149. (054b462)
- SENSECOLLE-40 — Raise the coverage floor — websocket reconnection, InfluxDB error branches, `main.py` secret masking. Coverage 70% → 78%, tests 149 → 197, floor raised to 78. (7ffc511)
- SENSECOLLE-36 — GUARD ESCALATION: canonical home for a collector's fake-upstream harness — ruled and shipped in luxarch 0.129.0; `FLEET-LAYOUT-STANDARD` now specifies `harness/` at the repo root declared in `[source].paths`. No repo change needed; we were already conformant.
- SENSECOLLE-37 — GUARD ESCALATION: `lint.source_declared` ignored `.gitignore` — fixed in luxlint 0.42.1 (clean-clone parity via `pathspec`). The rule had been unsatisfiable in both directions; the maintainer confirmed the same latent CI red on kidde-collector. (b9fb3b9 documented it while red; cleared on the 0.42.1 bump)
- SENSECOLLE-41 — GUARD ESCALATION: `--plan`'s un-adopted-overlay item could not be dispositioned — accepted and shipped in luxarch 0.135.0 as `[observability].exempt_reason`. Adopted here. (5c5519a, corrected in 744e63d)

Still open, did **not** ship: SENSECOLLE-35 (MFA/TOTP auth) — the only external ask, tracked as GitHub issue #8.

## Features

None. No new collector capability in this release.

## Refactors

- Narrowed exception handling across `client.py`, `websocket.py`, `influxdb.py`, `main.py`, `file_validator.py`. Introduced `MALFORMED_PAYLOAD` and `EXPORT_WRITE_ERRORS` tuples per module so the intent is greppable. Log-and-continue behaviour is unchanged; the change is that an `AttributeError`/`NameError` from a genuine defect now propagates instead of being logged as a transient failure. (SENSECOLLE-38, 02adaf4)
- Build backend `poetry-core` → **hatchling**, version `dynamic` from the `VERSION` file. Poetry stays the dependency manager in `package-mode = false`. The re-lock changed only the lock's `content-hash` — every resolved package version byte-identical. (08559a6)

## Schema / Migrations

None — this collector has no relational data layer (`repo.makefile_test_db_harness` and `repo.migrations_build_schema` are both correctly INERT).

## Infra / Ops

- **DESTRUCTIVE — deleted four legacy releases and their tags from the public remote:** `v1.0.1`, `v2.0.03`, `v2.0.06`, `v2.0.07`. They pointed at pre-squash, pre-transfer history whose `README.md` and `compose.yaml` carried the pre-transfer account's image path and an internal hostname (org-denylist *disclosure* tier — not credentials; the strings themselves are deliberately not repeated here, since the scrub commit quoting the token is its own leak). Backed up first: a full mirror clone of the remote plus each release's notes body as JSON. **This was a standing hollow green, not a regression** — the 2026.8.0 squash replaced `main` but left those tags on the old commits, so the history stayed publicly reachable; `make gitleaks` passed only because the local clone had never fetched tags, and `git fetch --tags` (release-runbook step 2) surfaced it. Any fresh clone would have red-ed. **Limits:** the repo has 4 public forks which retain that history regardless, and GitHub may serve unreferenced commits by SHA for a period — this removes the exposure from the canonical repo, not from the internet. `make gitleaks` now scans 35 commits clean. (11ba1e7)
- **Adopted the fleet release-docs standard** (luxarch 0.137.0, `repo.release_docs_present` + `repo.release_docs_current`): per-version `app/change_logs/{VERSION}.md` and `app/release_notes/{VERSION}.md` with the canonical writing-guides, plus root `CHANGELOG.md`/`RELEASE_NOTES.md` pointer indexes. This file is the first change log under it; earlier versions have none. (f3ff11e)
- **Dependency floors for four CVEs** (see Fixes). `poetry.lock` regenerated; the diff moved exactly `aiohttp 3.14.1 → 3.14.3` and `h2 4.3.0 → 4.4.1`, 43 packages before and after. (030c326)
- `VERSION` corrected to zero-padded CalVer (`2026.8.0` → `2026.08.0`) and bumped to `2026.09.0`.
- `make check` is now the canonical eight-step gate: `guard-version-check honest lint mypy test arch audit gitleaks`. New targets: `plan`, `guard-version-check`, `guard-upgrade`, `honest`, `mypy`, `status`, `onboard-check`; `format` is now `luxlint --format`.
- The mypy tail was removed — `make mypy` runs mount-only inside the luxlint image (the repo installs no lint tooling). Guard-status files (`.lux*-status.json`) are committed and regenerated with `make status`.
- Added the missing `.luxaudit.toml`; luxaudit had never actually run against this repo.
- One shared, GC-capped buildx builder (`luxardo-builder`, `--buildkitd-config` with a 20GB cap) replaces the per-project `sense-builder`.
- `compose.e2e.yml`: InfluxDB data moved to `tmpfs` instead of the image's implicit anonymous volume, which minted and orphaned a fresh volume on every run.
- Guard pins: luxarch 0.21.1 → 0.141.0, luxlint 0.10.0 → 0.44.2, luxaudit 0.1.8 → 0.4.0. Of note in the tail of that range: 0.141.0 supplied the headless-repo answer for release-notes surface badges (plain-text operator labels, no code registration — what this repo already did) and added `repo.compose_command_no_orphan_flags` (vacuous here); 0.140.0 standardised guard-escalation routing (tag `fleet-escalation`, title `[<guard> ESCALATION] …`) — the three escalations below predate that convention and used `guard-escalation`.

## Fixes

- **CVE-2026-69244, CVE-2026-69243, CVE-2026-59881 (aiohttp, floor `>=3.14.3`)** — aiohttp is the transport for every InfluxDB write. Out-of-bounds heap read in the C response parser while building an error message for a malformed response; request smuggling via WebSocket upgrades; client decompressing RSV1 frames with `permessage-deflate` never negotiated. Both packages were transitive with unbounded upper ranges, so a plain re-lock resolved back to the vulnerable versions — the floor has to be declared in our own manifest.
- **CVE-2026-71554 (h2, floor `>=4.4.1`)** — duplicate `Host` headers forwarded to the consumer, enabling smuggling where HTTP/2 is downgraded to HTTP/1.1. h2 is the HTTP/2 stack behind the Sense API client.
- **Health check DST bug** — `app/health/check.py` computed heartbeat age as `datetime.now() - datetime.fromtimestamp(mtime)`, both naive and container-local. Across a DST transition this shifts by an hour, so a live collector could report unhealthy or a stale one healthy. Both sides are now aware UTC.
- **Naive datetimes** in `websocket.py` (reconnect ETA) and `main.py` (startup banner) — now `datetime.now(UTC).astimezone()`: created aware, converted to the container zone only for display.
- **`app/core/config.py` logged through the root logger** (6 sites), so those records bypassed the per-area levels (`LOG_LEVEL_GENERAL`/`_API`/`_STORAGE`) and could not be filtered by source. Now a module logger.
- `harness/fake_sense.py` was first-party code the lint gate had never scanned; covering it surfaced 17 real mypy errors, including a heterogeneous dict literal inferring `object` for a float field so the load arithmetic was unchecked. Now a `TypedDict`; mypy clean.

## Notes

**Pre-deploy:** none. **Post-deploy:** none. No config, env-var or data-shape change — existing `.env` files, compose stacks and Grafana dashboards work unchanged, and no migration or backfill is required.

- Deployment is a straight image swap. Verified with the full hardware-free e2e (fake Sense → collector → InfluxDB, asserted) on the patched dependencies.
- **Release documentation** (`change_logs/` + `release_notes/` + root pointers) is established with this release per `luxarch --doc FLEET-RELEASE-PROCESS`; earlier versions have no per-version files.
- The narrowed exception boundaries and the six inline `# swallowed-exceptions:` waivers are deliberate — see `sense-collector`'s `.luxarch.toml`/`CLAUDE.md`. Do not widen them back without re-reading SENSECOLLE-38.
- `make test` measures coverage with `COVERAGE_CORE=sysmon` (luxlint 0.44.2): the default C tracer under-records lines inside some `async def` bodies, and the ratchet being monotonic-up means a too-low measurement would lock in a too-low floor. This app is async end to end, so it is the exposed shape. Verified both cores report 78% on Python 3.14, so the existing floor was already honest.
- Coverage floor is `[test].coverage_min = 78` in `.luxlint.toml`, monotonic up. `main.py`'s orchestration loop and `logging.py`'s handler setup remain the thin spots.
- The observability overlay is recorded as `[observability] exempt_reason` — a deliberate non-adoption, not a non-applicability. Parts of the fleet do run it.
